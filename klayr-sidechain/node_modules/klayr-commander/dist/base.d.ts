import { Command } from '@oclif/core';
export declare const defaultConfigFolder = ".klayr";
interface PrintFlags {
    readonly json?: boolean;
    readonly pretty?: boolean;
}
export default abstract class BaseCommand extends Command {
    static flags: {
        json: import("@oclif/core/lib/interfaces").BooleanFlag<boolean>;
        pretty: import("@oclif/core/lib/interfaces").BooleanFlag<boolean>;
    };
    printFlags: PrintFlags;
    finally(error?: Error | string): Promise<void>;
    init(): Promise<void>;
    print(result: unknown): void;
    printJSON(message: Record<string, unknown> | string): void;
}
export {};
