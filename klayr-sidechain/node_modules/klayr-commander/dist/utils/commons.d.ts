import { NETWORK } from '../constants';
export declare const klayrsnapshotUrl: (url: string, network: NETWORK) => string;
export declare const encryptPassphrase: (passphrase: string, password: string, outputPublicKey: boolean) => Promise<Record<string, unknown>>;
export declare const deriveKeypair: (passphrase: string, keyDerivationPath: string) => Promise<import("@klayr/cryptography/dist-node/types").Keypair>;
