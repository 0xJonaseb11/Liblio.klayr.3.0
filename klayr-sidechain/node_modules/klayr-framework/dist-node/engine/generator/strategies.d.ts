/// <reference types="node" />
import { TransactionPool } from '@klayr/transaction-pool';
import { Transaction, BlockHeader, BlockAssets, Event } from '@klayr/chain';
import { ABI } from '../../abi';
export declare class HighFeeGenerationStrategy {
    private readonly _abi;
    private readonly _pool;
    private readonly _constants;
    constructor({ abi, pool, maxTransactionsSize, }: {
        readonly abi: ABI;
        readonly pool: TransactionPool;
        readonly maxTransactionsSize: number;
    });
    getTransactionsForBlock(contextID: Buffer, header: BlockHeader, assets: BlockAssets): Promise<{
        transactions: Transaction[];
        events: Event[];
    }>;
}
