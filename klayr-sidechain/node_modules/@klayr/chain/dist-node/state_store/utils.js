"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toSMTKey = exports.copyBuffer = void 0;
const cryptography_1 = require("@klayr/cryptography");
const constants_1 = require("../constants");
const copyBuffer = (value) => {
    const copiedValue = Buffer.alloc(value.length);
    value.copy(copiedValue);
    return copiedValue;
};
exports.copyBuffer = copyBuffer;
const toSMTKey = (value) => Buffer.concat([
    value.subarray(1, constants_1.SMT_PREFIX_SIZE + 1),
    cryptography_1.utils.hash(value.subarray(constants_1.SMT_PREFIX_SIZE + 1)),
]);
exports.toSMTKey = toSMTKey;
//# sourceMappingURL=utils.js.map